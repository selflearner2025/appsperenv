apiVersion: v1
kind: ConfigMap
metadata:
  name: violations-management-app-cm
data:
  artifactName: bpmn-poc.tar.gz
  artifactSha256: "changing repo b"
  # command sha256sum bpmn-poc.tar.gz
  artifactUrl: "abcd.efgh"
